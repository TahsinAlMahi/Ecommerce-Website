<div class="card mr-2 mb-2" style="width: 15rem;">
  <div class="card-body">
    <h5 class="card-title">{{this.title}}</h5>
    <p class="card-text">{{this.price}}</p>
    <a href="/products/{{this.category}}/{{this.slug}}" class="btn btn-primary">View Details</a>
  </div>
</div>